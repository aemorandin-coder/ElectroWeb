# C-07 — Limpieza del carril Claude
Estado: HECHO
Rama: claude/C-07 (desde `main` integrado)

## Qué cambió
- **11 archivos borrados**, cada uno con **0 importadores**. Lo verifiqué resolviendo cada import real (alias `@/`, relativos, `import()` y `require()`), no con grep de texto:
  - `lib/utils.ts`, `lib/validation.ts`, `lib/reviews.ts`, `lib/pago-movil/index.ts`
  - `components/ui/Card.tsx`, `components/ui/GlossyIcon.tsx`, `components/ui/ProductImage.tsx`, `components/ui/index.ts`
  - Plantillas de email `OrderStatusUpdate`, `PasswordReset` y `Welcome` (el plan pedía revisarlas antes de decidir): nadie las importa. `email-service` arma esos correos con sus propias plantillas. Quedan en el historial de git por si se quieren recuperar.
- **`console.log` en `app/api/**` y `lib/**`:** de 29 a **0**. Se quitaron con el AST de TypeScript, solo sentencias completas; no hubo casos especiales, como un `if` sin llaves.
  - 27 borrados. Algunos **filtraban datos en los logs**: teléfono y referencia de Pago Móvil en la llamada a BDV, rutas absolutas del servidor en uploads y el payload completo del webhook de SADES.
  - 2 convertidos a `console.warn` porque avisan de algo real: correo en "modo prueba" redirigido y evento de webhook no manejado.
  - `console.error` y `console.warn` se conservan.
  - Se quitaron las variables que solo usaban esos logs (`fileExists` en uploads; `senderName` y `giftAmount` en `users/check`) y las líneas en blanco duplicadas que dejó el borrado.
- **`public/uploads/`:** no se saca del índice (decisión de Andrés, siguiendo la recomendación). `.gitignore` ya lo ignora, y sacar los 40 archivos viejos haría que un `git pull` en el servidor los borrara.

## Cómo se verificó
- `npx tsc --noEmit` → 0 errores. `npm run build` → OK.
- `eslint` en los 13 archivos modificados: igual que `main`, 0 problemas nuevos.
- `grep -rn "console\.log" app/api lib` → 0.
- `next dev`: `/`, `/productos`, `/api/products/public` y `/api/settings/public` → 200.

## Hallazgos (sin arreglar)
- **`POST /api/users/check`** responde "Invitación enviada a …" **sin enviar ningún correo** (hay un `TODO`; los logs simulaban el envío). Hay que implementar el envío o cambiar el mensaje.
- **Módulos sin importadores que quedan:** `components/ui/ErrorState.tsx`, `components/ui/Modal.tsx` y `components/ui/StatusBadge.tsx` (solo los exportaba el `index.ts` borrado, que nadie usaba). Candidatos a borrar en C-40. `lib/queries/home.ts` es de C-13 y lo usará C-22.
