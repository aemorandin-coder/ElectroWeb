# C-100 — Envíos con ZOOM y MRW (fase 1)
Estado: HECHO EN CÓDIGO — **falta la prueba de punta a punta y el `db push`**
Rama: `claude/C-100` (desde `claude/C-99`). Decisiones de Andrés del 22/09: cobro a destino, envío gratis por producto con badge, paquete completo gratis si lo lleva, y delivery en Guanare con tarifa fija.

> El commit lo hizo Gemini desde la carpeta principal mientras Claude todavía trabajaba, y puso "HECHO". Esta es la corrección: el código está completo y compila, pero **no se ha probado con datos**.

## Qué cambió
- **`lib/envios/`** (nuevo): `empresas.ts` (empresas, estados, paso siguiente de cada orden), `zoom.ts` (API pública de ZOOM: ciudades, oficinas, tarifa y rastreo, con caché y tiempo límite), `mrw.ts` (agencias con copia guardada), `destino.ts` (valida el destino contra las listas reales) y `seguimiento.ts` (rastreo automático y avisos).
- **Checkout:** `components/checkout/EntregaEnvio.tsx` (nuevo) con empresa, oficina real, dirección, quién recibe y la tarifa de referencia de ZOOM. Se fueron 754 líneas de la página.
- **Cálculo (`lib/pricing.ts`):** cobro a destino (solo embalaje), envío gratis por producto o por monto, delivery en Guanare y **el umbral ya no cuenta los digitales** (E1).
- **Órdenes:** destino, destinatario y quién paga el flete se guardan en la orden; historial en `ShipmentEvent`; el servidor no deja enviar sin pago (E4); cada tipo de entrega tiene sus estados (E11); el enlace de rastreo lo arma el servidor (E9, E12); el pedido digital se cierra al entregar el último código (E13); el correo dice el tipo de entrega real (E14).
- **Panel:** paso siguiente según la entrega, formulario de guía con la empresa del cliente y "Copiar datos para la guía".
- **Cliente:** destino, aviso de cobro a destino, copiar guía, rastreo e historial. Aviso automático cuando el paquete llega a la oficina (cumple lo que promete el checkout, E10).
- **Productos:** interruptor "Envío gratis" en el wizard y badge en tarjeta, ficha y carrito.
- **Seguridad encontrada al pasar:** `GET /api/orders` le mandaba al cliente el costo y la referencia del proveedor y las notas internas (ahora lista blanca); una cuenta SUPPORT sin permiso veía todas las órdenes; el checkout esperaba 8 segundos simulados.

## Cómo se verificó
- `npx tsc --noEmit`: 0 errores. `npm run build`: OK (los avisos de "columna no existe" son de la base local, que está atrasada).
- ESLint por archivo contra `main`: ningún archivo empeora; los 12 archivos nuevos quedan en 0.
- **APIs de ZOOM probadas de verdad** (21 y 22/09): producción pública en `https://api.zoom.red/canguroazul` (69 oficinas en Caracas, tarifa de Guanare a Caracas y rastreo).

## Falta
- `prisma db push` en la base local y en la tienda de ejemplo (el entorno bloqueó el comando a Claude).
- Prueba de punta a punta en el navegador: carrito → checkout con ZOOM y con MRW → panel "Enviado" → rastreo del cliente, y gift card → código → raspado.
- Deploy: `db push`, variable `CRON_SECRET` y una línea de cron cada 2 h a `/api/cron/envios`. Configurar la tarifa del delivery de Guanare y activarlo.
