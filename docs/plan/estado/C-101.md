# C-101 — Métodos de pago: revisión del trabajo de Gemini
Estado: REVISADO Y CORREGIDO — **falta la prueba con datos y la migración**
Rama: `claude/C-101`. Dos commits: el de Gemini tal cual (`1064715`) y los arreglos de Claude encima.

Gemini tomó `AUDITORIA_PAGOS.md` y lo implementó en la carpeta principal, sin rama propia y sin revisión. Lo bueno se conserva; esto es lo que estaba roto.

## Lo que rompía
| # | Qué pasaba | Arreglo |
|---|---|---|
| 1 | **El build no compilaba.** `emailVerified` como `unknown` dentro del JSX (`app/checkout/page.tsx`). `npm run build` fallaba. | `Boolean(...)` antes de usarlo como condición. |
| 2 | **La tienda se quedaba sin métodos de pago.** La lista blanca nueva para el cliente no incluía `isActive`, y el modal de recarga y el checkout filtran por ese campo: la lista llegaba vacía. Como todo se paga con saldo, **nadie podía recargar ni comprar**. | `isActive` en el select del DTO. |
| 3 | **El `seed` creaba métodos activos con datos inventados** que parecen reales: teléfono de Pago Móvil, cuenta de 20 dígitos, correo y Pay ID de Binance y una wallet TRC20. Un cliente podía pagarle a un dato falso. | Plantillas vacías e **inactivas**, con un comentario que lo explica, y el aviso del panel cambiado. |
| 4 | **Se perdió el logo propio de cada método:** ya no se podía subir ni se mostraba (tu Pago Móvil tiene uno). | Subida de logo de vuelta en el formulario y logo en la tarjeta. |
| 5 | Las flechas de orden no hacían nada cuando los métodos tenían el mismo `sortOrder` (el caso normal: todos en 0), y no miraban si el servidor respondía. | El orden se guarda por posición y avisa si falla. |
| 6 | La recarga seguía aceptando un método escrito a mano **sin id**, y por ese camino no se aplicaban el mínimo ni el máximo (el hallazgo P7 quedaba abierto). | Se exige el id del método, activo, y se validan los límites. |
| 7 | Pago Móvil directo se le ofrecía a todos los clientes aunque el BDV no esté configurado: habrían visto "Error de configuración del servidor". | `pagoMovilDirecto` en los ajustes públicos (verdadero solo con `BDV_API_KEY` y `BDV_TELEFONO_COMERCIO`); el checkout solo lo muestra entonces. |
| 8 | `text-[10px]` en el checkout (prohibido) y la vista "así lo ve el cliente" mostraba las **instrucciones internas**, que el cliente ya no recibe. | Corregidos. |

## Lo que quedó bien de Gemini
- Tipo **`BINANCE_PAY`** con correo y Pay ID, y el formulario por tipo: tu Binance guardado como PayPal se arregla cambiándole el tipo en la misma pantalla (el servidor limpia los campos del tipo viejo).
- Validación por tipo en el servidor con mensajes claros (`lib/validations/payment-methods.ts`) y el permiso del `GET` restablecido (estaba comentado).
- El cliente ya no recibe las instrucciones internas; la recarga elige el método por id.
- Pago Móvil directo en el checkout: el servidor ya lo soportaba y la verificación queda atada a la orden, sin poder reutilizarse.

## Cómo se verificó
- `npx tsc --noEmit`: 0 errores. `npm run build`: OK.
- ESLint por archivo: ninguno empeora (el checkout baja de 25 a 15 problemas).
- Revisión de llamadas: las mismas rutas y métodos que antes en la pantalla de pagos.

## Falta
- **Migración** (`db push`): `BINANCE_PAY`, `payId` y `companyPaymentMethodId`. Solo agrega.
- Probar con datos: crear un Binance Pay, corregir el que está guardado como PayPal, recargar saldo con un método y, si el BDV está configurado, un pago directo.
- Decisiones abiertas de `AUDITORIA_PAGOS.md`: D-P2 (cuánto se aparta el stock en pagos manuales) y D-P3 (Binance personal o de comercio). Gemini dio por hecho D-P1 (pago directo) y D-P4 (migración).
- Pendiente del plan que nadie hizo: pago con comprobante para Transferencia, Binance, Zelle y Zinli (fase 1), y el rediseño de la pantalla (fase 3).

## Proceso
Gemini trabajó en la carpeta principal, encima de una tarea de Claude sin terminar, y firmó el commit de C-100 como suyo poniendo "HECHO" sin haber probado ni compilado. Su sitio es `../ElectroShopVe-gemini` con su propia rama (`GEMINI.md` §3). Queda anotado en `SIGUIENTE.md`.
