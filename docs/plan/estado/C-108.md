# C-108 — Revisión de Gemini R20, R21 y R22
Estado: HECHO — **R20 y R22 aprobadas; G-67 (R21) rechazada como fuente**
Rama: `claude/C-108` (solo documentos). Las ramas de Gemini están encadenadas: mergear `gemini/R22` trae R20, R21 y G-68.

Andrés sospechó de lo rápido que terminó Gemini (24/09). Se revisó todo, no solo lo último.

## R20 (G-62…G-66) · aprobada
- Código leído completo (`git diff 956c7aa gemini/R20`, sin `docs` ni el archivo borrado):
  - G-62: `whitespace-pre-line` en `ConfirmDialog` y `businessVerificationNotes` en el perfil.
  - G-63: `ProductForm.tsx` borrado; `git grep ProductForm` en `app`, `components` y `lib` → 0.
  - G-64: `window.confirm` de gift cards → `useConfirm`, mismo texto. El diálogo usa la misma capa que el modal de la hoja (`--z-modal`) y se monta después en el DOM, así que sale encima (igual que en G-58).
  - G-65: `any` → tipos, `catch` sin variable y comillas escapadas. No cambió la lógica.
  - G-66: 3 montos con `formatUSD`. `formatUSD` maneja negativos, así que la ganancia negativa del wizard se muestra bien.
- Sin cambios de `fetch`, URLs, cuerpos ni cálculos.

## R22 (G-68) · aprobada
- Los 5 archivos quedaron **idénticos byte a byte** a la versión que Claude probó en memoria antes de escribir la tarjeta.
- Diff ordenado contra `gemini/R21` vacío en los 5: solo se movieron líneas (36 y 36).
- ESLint, corrido por Claude: `react-hooks/immutability` en 0, y queda lo que la tarjeta anticipaba (`set-state-in-effect` en los 5; `purity` en garantía).
- `npx tsc --noEmit`: salida 0, corrido por Claude.
- Fue rápida porque eran 5 bloques para mover, no por hacerla mal.

## R21 (G-67) · rechazada como fuente
El informe declara con honestidad "QA con datos pendiente", pero **inventa detalles** que presenta como leídos del código:
- De 49 textos citados entre comillas, **21 no existen** en `app`, `components` ni `lib`, aun buscando sin distinguir mayúsculas. Ejemplos: "Hash de Integridad: SHA-256", "Protección Legal: Activa", "Reportes y Estadísticas" (el título real es "Reportes"), "Requieren atención inmediata", "Descargar HTML", "He leído y acepto los Términos y Condiciones".
- Describe una respuesta de `GET /api/admin/discount-requests` con `pagination` y un filtro `search`; la API no tiene ninguno de los dos.
- Los números de línea no coinciden ("Razon del rechazo" en la 390; está en la 412).
- **Consecuencia:** C-102, C-103 y C-104 no se basan en G-67. Claude lee esas pantallas de primera mano al empezar cada tarea.
- Se anotó en `GEMINI.md` como regla nueva: no se cita un texto que no se haya copiado de un `grep`.

## Decisiones de Andrés (24/09) registradas
- **C-96:** autorizado el redondeo de los saldos existentes. Lo corre Andrés en el servidor: Claude no tiene acceso a producción (pasos en `estado/C-96.md`).
- **Duplicar producto:** opción A (el servidor copia todo). Va dentro de C-51.
- **MRW fase 2 (cuenta corporativa):** por ahora no. La guía de MRW se carga a mano en el panel, como hoy.
- **Clientes borrados (C-92):** en el panel solo ve los productos archivados (los que tenían órdenes). Las órdenes de esos clientes están en Órdenes con la etiqueta "Cliente eliminado" (G-57).
