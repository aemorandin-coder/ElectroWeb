# C-22 — Home vitrina (tipo Best Buy)
Estado: HECHO
Rama: claude/C-22 (desde `main` 94f678b, con C-20 integrado)

## Qué cambió
- **`app/page.tsx` reescrito** según `PLAN.md` §3. Es un Server Component que lee todo en paralelo con `lib/queries/home.ts` (C-13) y `getPublicSettings`. Sin fetch a la propia API y con `revalidate = 60`. Solo son cliente el carrito, compartir, las flechas de las filas y el buscador del header.
- **Orden de la página:**
  1. Chips de categorías (móvil).
  2. **Vitrina "Destacados de la semana"**.
  3. "Compra por categoría" (desktop).
  4. Ofertas.
  5. Lo más vendido.
  6. Franja de gift cards.
  7. Recién llegados.
  8. Las 3 categorías con más productos.
  9. "Compra con confianza".
  10. "Más de ElectroShop" (servicios, cursos, creadores).
  11. Video, si está activo.
  12. Footer y popup.
- **Vitrina:** el primer pantallazo siempre muestra productos con precio y botón.
  - **Móvil:** una fila deslizable (tarjeta a 78vw, se asoma la siguiente).
  - **Desktop:** producto estrella (`FeaturedHeroCard`) y al lado una fila deslizable con el resto: 2 tarjetas visibles en `lg` y 3 en `xl`. Todo tiene la altura de una tarjeta, así que cabe entero en la primera pantalla. Una primera versión con grilla 2×2 medía ~1000px y se descartó al verla en capturas.
  - **Relleno:** si hay pocos destacados, la vitrina se completa con ofertas y recién llegados, sin repetir productos.
- **Componentes nuevos en `components/home/`:**
  - `FeaturedShowcase`, `FeaturedHeroCard`.
  - `ProductSection`: se oculta con menos de 4 productos, para no dejar filas cojas.
  - `CategoryChips`, `CategoryRail`, `DigitalStrip`, `TrustBar` (el enlace de WhatsApp sale de settings), `MoreFromElectroShop`.
- **`components/ui/productCardData.ts`:** nuevo `getStockLabel`, compartido por `ProductCard` y la tarjeta estrella ("Entrega digital", "Quedan N", "En stock").
- **`components/ui/ProductShelf.tsx`:** la variante `featured` muestra 2 tarjetas en `lg` y 3 en `xl`. Solo la usan la vitrina y `/dev/ui`.
- **Borrados:** `components/home/HomeSearchBar.tsx` (placeholder que re-renderizaba para siempre, AUDITORIA §7) y `components/home/ProductCarousel.tsx` (B11, `window.innerWidth`). Ya no los importaba nadie.
- **Settings respetados:**

  | Setting | Efecto |
  | --- | --- |
  | `maxFeaturedProducts` | Tamaño de la vitrina, mínimo 5 |
  | `showCategories` | Muestra u oculta los chips y el rail |
  | `maxCategoriesDisplay` | Cuántas categorías salen |
  | `lowStockThreshold` | Umbral del aviso "Quedan N" |
  | `autoHideOutOfStock` | Se aplica en las queries de C-13 |
  | `heroVideoEnabled/Url/Title/Description` | Sección de video |

- **Sin productos publicados:** mensaje "Estamos preparando el catálogo" con botón a `/productos`.
- Un `<h1>` `sr-only` con el nombre de la empresa (el header ya no tiene h1 desde C-20).

## Cómo se controla desde el admin (sin cambios en el panel)
- **Destacados:** la casilla ⭐ "Destacado" (`isFeatured`) al crear o editar el producto.
- **Ofertas:** automáticas cuando el producto tiene "precio antes" (`compareAtPriceUSD`) mayor que el precio. Se ordenan por % de ahorro.
- **Lo más vendido:** automático, por unidades vendidas en 90 días. No cuenta órdenes canceladas ni reembolsadas.
- **Recién llegados:** automático, los últimos publicados.
- **Categorías del home:** las que tienen más productos publicados.
- **Configuración → portada:** "Máximo productos destacados en portada", "Mostrar carrusel de categorías", "Máximo categorías a mostrar" y el video.

## Cómo se verificó
- `npx tsc --noEmit`: 0 errores. `eslint` sobre los 12 archivos tocados: limpio.
- `npm run build`: OK contra la BD local y OK contra el esquema de demostración. `/` sale como estática con revalidación de 1 minuto.
- **Demo visual** (autorizada por Andrés):
  - Esquema temporal `c22_demo`: 21 productos con las imágenes de `public/uploads/products`, 6 categorías, 1 gift card digital con montos y 4 órdenes para "Lo más vendido".
  - `next start -p 3100` apuntando a ese esquema.
  - Capturas con Firefox headless a 360, 768, 1024 y 1440, más la página completa a 360 y 1440.
  - Al terminar se apagó el servidor, se hizo `DROP SCHEMA c22_demo CASCADE` (en la BD solo queda `public`) y se reconstruyó normal.
- **QA 360px:** el primer pantallazo muestra header, buscador, chips y la tarjeta completa del primer producto (imagen, precio USD y Bs., stock y botón "Agregar").
- **Peso del HTML del home con 21 productos:** 500 KB sin comprimir, 44 KB con gzip.

## Pendiente
- En móvil la barra inferior tapa el final de la página: es **C-21** (padding con `--bottom-nav-h`).
- El popup del home sigue igual hasta **C-23**.
- "Ver todo" de Ofertas y Lo más vendido no lleva a un listado filtrado: depende de **C-30** (orden y filtros por URL en `/productos`).
- Los íconos de categoría usan los nombres del selector del admin. En la demo tres categorías mostraron el ícono genérico porque sus nombres de ícono no están en esa lista (dato de prueba, no es un bug).
