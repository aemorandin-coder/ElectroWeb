# C-26 — Vitrina "Destacados de la semana" con fondo azul suave
Estado: HECHO
Rama: claude/C-26 (desde `main` 9d715fb)

## Qué cambió
- **Pedido de Andrés:** la vitrina se confundía con el resto del home (gris `surface`). Ahora usa el azul más suave de la paleta y queda separada de "Compra por categoría" (blanco) y de "Ofertas" (gris).
- **`components/home/FeaturedShowcase.tsx`:** fondo `bg-surface` → `bg-brand-100` (#eaeffa).
- **`components/home/CategoryChips.tsx`** (solo móvil, va pegado encima de la vitrina):
  - Mismo fondo, para que no quede una franja gris entre ambos.
  - El chip "Todas" pasa de `bg-brand-50` a blanco con borde `brand-200`, porque sobre el azul ya no se distinguía.

## Cómo se verificó
- **Contraste sobre #eaeffa:** título `ink` 13,4:1, "Ver todo" `brand-500` 4,8:1, textos `muted` 4,6:1. Las tarjetas siguen blancas.
- **Tienda de ejemplo** (esquema `c26_demo`, borrado al final) + Firefox a 1440, 1024, 768 y 360 px:
  - Fondo `rgb(234, 239, 250)` en la vitrina y, bajo `lg`, también en los chips.
  - Sin scroll horizontal.
- `npx tsc --noEmit`: 0 errores. ESLint de los 2 archivos: limpio. `npm run build`: OK.

## Pendiente
- Nada.
