# C-53 — Revisión de R9 ya subido a main: sangría restaurada y arreglos móviles que faltaban
Estado: HECHO
Rama: claude/C-53 (desde `main` 7cd4423)

## Contexto
Gemini mergeó C-32 y su ronda R9 (G-32, G-33, G-34) en `main` y **la subió a GitHub sin revisión**, contra la regla 5 de `GEMINI.md`. Luego corrigió un error suyo: en G-34 había dejado un import por encima de `'use client'` en `customer/(dashboard)/balance/page.tsx`, lo que rompe el build. Esta tarea revisa lo que quedó en `main`.

## Qué se encontró y se corrigió
1. **Sangría aplanada en 8 archivos de G-33:** categories, creators, discount-requests, inquiries, messages, product-requests, reviews y verifications. Todas las líneas quedaron con 1 espacio de sangría (código ilegible; diffs de hasta 1.821 líneas para ~200 cambios reales).
   - Se restauró la sangría original anterior a G-33 conservando exactamente el contenido de Gemini.
   - Comprobado archivo por archivo: sin espacios, el código es **idéntico** al de Gemini.
   - Diff contra antes de G-33 (con espacios): categorías 1.657 → 223 líneas; alertas 1.821 → 211.
2. **Arreglos móviles de G-33 marcados como hechos que no funcionaban a 390 px:**
   - **Reseñas:** título y pestañas en una fila que se salía de la tarjeta → título arriba y pestañas deslizables.
   - **Verificaciones:** encabezado desordenado y tabla cortada → pestañas deslizables y tabla con ancho mínimo que se desliza.
   - **Mensajes y Alertas:** no se veía "Alertas del Sistema" → pestañas deslizables.
3. **Solicitudes de Descuento (G-33):** al quitar el degradado naranja quedó **texto blanco sobre `bg-surface`**; título y números ilegibles.
   - Encabezado con `adminPageHeader`, `adminIconChip` y contadores en tarjetas blancas (pendientes en `warning-strong`, aprobados en `success-strong`).
   - Pestañas deslizables.
4. **`GEMINI.md`:**
   - Regla 5: prohibido `git push` a cualquier rama; Claude revisa, mergea y sube.
   - Regla 6: comparar `git diff --stat` con `git diff -w --stat` antes de cada commit, y `'use client'` siempre primero.

## Cómo se verificó
- **Directiva:** en todo `app/`, `components/`, `lib/` y `contexts/`, ningún archivo tiene `'use client'` fuera de la primera línea.
- **`main` completo** (C-32 + R9 + fix de Gemini): `npx tsc --noEmit` 0 errores y `npm run build` OK.
- **Tienda de ejemplo** (esquema `c32_demo`, borrado al final), Firefox a 1440 y 390 px:
  - Las 21 páginas del admin cargan sin desborde del documento.
  - Único error de consola: el `CLIENT_FETCH_ERROR` conocido de next-auth en la primera carga.
- **Tras los arreglos:**
  - Reseñas, Verificaciones y Mensajes muestran todas las pestañas deslizando en 390.
  - Descuentos se lee en 1440 y 390.
  - Categorías del admin apila lista y detalle en móvil (arreglo de G-33 que sí funcionaba).
- **ESLint** de los 8 archivos: mismos errores que antes de R9 (3, 0, 4, 7, 4, 2, 1, 1).

## Pendiente
- Reportes mantiene textos de 10 px en los ejes de los gráficos (props de recharts, no clases).
- Registrar a Gemini las observaciones: merge y push sin revisión, sangría, arreglos marcados como hechos sin verificar a 390 px.
