# C-84 — Registro más fácil, mismas reglas en pantalla y servidor, perfil blindado
Estado: HECHO
Rama: claude/C-84 (desde `main` con C-75)

Análisis completo para Andrés y plan de Google/Facebook/Apple: `docs/plan/AUDITORIA_REGISTRO.md`.

## Qué cambió
- `lib/validations/registro.ts` (nuevo): reglas únicas de nombre, correo, teléfono, cédula, contraseña y términos. Lo usan la pantalla y la API, así que ya no pueden contradecirse. Guarda la cédula como `V-12345678` y el teléfono como `+58 4121234567`. Incluye `sugerirCorreo` (gmial → gmail…).
- `lib/rutas.ts` (nuevo): `rutaInternaSegura` (antes copiada en el login; el registro aceptaba `//otro-sitio.com`) y `esRutaDeAcceso`.
- `components/auth/AuthShell.tsx` (nuevo): marco de las páginas de acceso. En el teléfono va sin tarjeta, fondo blanco, para que el captcha de 303 px quepa a 360 px; desde `lg` es una tarjeta.
- `app/registro/page.tsx` reescrito: una columna, sin "repetir contraseña", errores fijos bajo cada campo (antes globos que se borraban a los 3 s), foco en el primer error, ayudas visibles (antes solo con hover), reglas de contraseña marcadas mientras se escribe, selector nativo de país y de tipo de documento con teclado numérico, botón activo aunque falte el captcha, términos y privacidad en otra pestaña, lee `callbackUrl` y `redirect` (Comparte y gana usa `redirect`), inicia sesión con el correo que devuelve el servidor y, si falla, muestra "Tu cuenta está lista" en vez de un error rojo.
- `app/api/auth/register/route.ts`: usa `registroSchema`, exige `acceptTerms`, devuelve `field` en cada error (también `email` al repetir y `captcha`), devuelve el correo normalizado y ya no el objeto del usuario, mensajes con tildes, sin `any`.
- `app/login/page.tsx`: mismo marco, `h1`, errores fijos, `autoComplete="current-password"` (antes `password`, que no existe), captcha sin relleno extra, "Crea una aquí" conserva el destino, íconos Fi.
- `app/api/user/profile/route.ts` (PUT): esquema zod. La cédula solo se escribe si estaba vacía (antes la API la dejaba cambiar aunque la pantalla la bloquea, y es la que firma los términos del saldo). Nombre y teléfono se validan con las reglas del registro solo si cambian. Imagen: `/uploads/…`, `/api/uploads/…` o `https://` (antes cualquier texto, incluso `javascript:`).
- `components/public/MobileNavBar.tsx`: oculta en login, registro, recuperar contraseña y verificar correo (tapaba la contraseña) y en `/creator/dashboard` (quedaba encima del menú del creador). `components/WhatsAppButton.tsx`: baja a la esquina en esas páginas.

## Verificación
- `npx tsc --noEmit` 0 errores · `npm run build` OK · ESLint: 0 problemas en los archivos nuevos; `app/login/page.tsx` pasa de 7 a 5 (los 5 son de `main`).
- API contra el servidor (`next start`, esquema `rev10_demo`, hCaptcha simulado), 19 de 19:
  registro normal con correo en mayúsculas, teléfono `0412-123 4567` y cédula `v 12.345.678` → 201 y guardado normalizado; entra con el correo devuelto; contraseña de 6 → 400 `password`; pasaporte `P-AB123456` → 201; sin términos → 400 `acceptTerms`; correo repetido con otra capitalización → 400 `email`; fijo `0212` → 400 `phone`; sin captcha → 400; guardar perfil como lo manda la pantalla → 200; cambiar la cédula → 400; la misma cédula escrita distinto → 200; imagen `javascript:` y `//evil.test` → 400; avatar subido → 200; nombre con HTML → 400; teléfono nuevo normalizado y `bio: null` → 200; sin cédula se puede escribir una vez → `E-8123456`.
- Navegador (Firefox, WebDriver BiDi) a 360 y 1440: sin desborde horizontal; enviar vacío marca 5 errores, enfoca "Nombre" y siguen visibles a los 3,5 s; el captcha queda dentro de la pantalla (x 23 → 325 a 360); teclado `tel` en teléfono y `numeric` en cédula; letras borradas del teléfono y de la cédula; sugerencia `ana@gmial.com` → clic → `ana@gmail.com`; `?callbackUrl=//evil.test` → "Volver" va a `/`; `?redirect=/cursos` → el enlace a login lleva `callbackUrl=%2Fcursos`; login a 360 y 1440 con `h1`, sin barra inferior.

## Pendiente
- Decisiones de Andrés (en `AUDITORIA_REGISTRO.md` §2 y §3): cédula fuera del registro, teléfono opcional, y las 3 de Google.
- C-85 Google cuando Andrés cree el cliente OAuth.
- C-80: límite de intentos de login en el servidor, cuentas desactivadas que pueden entrar, `GET /api/user/profile` sin DTO.
- `app/recuperar-contrasena` y `app/verificar-email` todavía con el marco viejo → ChatGPT R1.
- Carril: `app/login`, `app/registro`, `components/auth/` y `lib/validations/registro.ts` pasan al carril Claude (seguridad y OAuth). `app/recuperar-contrasena` y `app/verificar-email` pasan al carril ChatGPT para R1. `PLAN.md` §4 y `GEMINI.md` actualizados.
