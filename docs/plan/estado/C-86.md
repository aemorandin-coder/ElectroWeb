# C-86 — Revisión de `gemini/R10` (G-38, G-39) y `gemini/R11` (G-40…G-43)
Estado: HECHO
Rama: claude/C-86 (merge de `gemini/R11`, que incluye los dos commits de R10, + arreglos)

## Cómo se revisó
- 43 archivos, +4.011/−4.232. Todos dentro del carril Gemini o de las excepciones de R11.
- Script que borra los `className` y la sangría de las dos versiones y compara el resto (`sin_clases.py`): así se ve solo la lógica que cambió. Revisado archivo por archivo, con más detalle en carrito, checkout, pago móvil, saldo y modales.
- `npx tsc --noEmit` 0 errores · `npm run build` OK · ESLint por archivo contra `main`: ningún archivo empeora después de los arreglos (carrito 4→1, garantía 6→3, favoritos 12→7, notificaciones 3→0).
- Navegador a 390 y 1440 con sesión de cliente: carrito con un producto y una gift card, checkout, inicio del panel, pedidos, saldo, perfil, direcciones, favoritos, garantía, referidos, configuración, notificaciones, reseñas y 404. Sin "Algo salió mal" y sin desborde horizontal.

## Lo que estaba mal y se arregló aquí
1. **Carrito: todas las gift cards salían con el mismo diseño.** G-40 leía `(item as any).design`, un campo que el carrito no guarda (el diseño va en el id `gift-card-<diseño>-<fecha>`), con un mapa de diseños viejo. Ahora usa `getGiftCardDesign` de `lib/gift-card-designs.ts`. Verificado: una gift card "navidad" sale verde y roja.
2. **Carrito: bolívares con una tasa inventada.** Pedía otra vez `/api/settings/public` y, si fallaba, calculaba con 50 Bs por dólar. Ahora usa `useSettings()` (la tasa que el layout leyó en el servidor). Verificado: 25 USD → Bs. 912,50 con la tasa 36,5.
3. **Carrito:** botones de cantidad con `-` y `+` de texto y 32 px → íconos `FiMinus`/`FiPlus` de 40 px.
4. **Configuración del cliente: "Empresa" se podía elegir sin verificación.** G-39 quitó el `disabled`. El servidor lo rechaza al guardar, así que el cliente veía un error sin entender por qué. Restaurado: deshabilitado y atenuado.
5. **Garantía: se perdió el bloque "¿Necesitas ayuda?".** Tenía un correo y un teléfono inventados y G-39 lo borró entero. Ahora muestra el WhatsApp y el correo de Configuración, solo si existen.
6. Imports y variables sin uso que dejó R11 (`goToBusinessAndDismiss`, `adminLabel`, `adminSuccessButton`).

## Aceptado, con nota
- **Sangría:** otra vez se reindentaron archivos enteros (referidos 1.363 líneas de diff contra 231 sin espacios; garantía 780/210; reseñas 329/77; `CustomerOnboarding` 324/86; `DocumentUpload` 269/37). No rompe nada, pero ensucia la historia. Regla 6 de `GEMINI.md` reforzada.
- Direcciones: G-38 agregó validación en el navegador (campos requeridos y agencia de ZOOM/MRW) con toast. Es lógica, no estaba permitida, pero es correcta y el servidor sigue validando.
- Garantía: el botón de un pedido fuera de garantía ya no abre el formulario. Correcto.

## Incompleto en R11 → Gemini R12
- **G-41 no terminó el checkout:** quedan 55 clases de la paleta vieja en `app/checkout/page.tsx` (el resumen del pedido y la información de contacto), 6 en `CheckoutPagoMovilForm.tsx` y 5 en `VerificarPagoMovilForm.tsx`. El resumen sigue mostrando "USD 50,00$" en vez de `formatUSD`, y 5 `toFixed` en montos.

## Encontrado al pasar (no es de Gemini)
- **Gift card sin saldo = callejón sin salida** (`app/gift-cards/page.tsx`): si el cliente no tiene saldo suficiente, la gift card va al carrito, pero el checkout rechaza gift cards ("Las gift cards no se pueden pagar desde el checkout", `lib/order-quote.ts`). Además la compra con saldo espera 9 segundos de pasos simulados con `setTimeout`. → **C-87** (decisión de Andrés: recargar saldo primero o pagar la gift card por el checkout).
- Panel del cliente a 390 px: el número de pedido se parte en tres líneas y la barra inferior deja ver el contenido detrás. El perfil abre la hoja "¿Sabías que…?" apenas entra. → jerarquía del panel del cliente (ChatGPT R2).
