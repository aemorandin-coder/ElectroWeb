# C-90 — Revisión de `gemini/R12` (G-44…G-46)
Estado: HECHO
Rama: claude/C-90 (merge de `gemini/R12` sobre `main` con C-89, + arreglos)

## Resultado de la revisión
**R12 salió bien.** Es la primera ronda de Gemini sin cambios de lógica fuera de lo permitido y sin sangría cambiada.
- 14 archivos de código dentro del carril de R12.
- Clases de la paleta vieja (grep de la tarjeta): **258 → 0**. Checkout 63→0, `StatusBadge` 37→0, `UserAccountButton` 40→0, `Button` 21→0, `Footer` 20→0, `Badge` 18→0, `LoadingSpinner` 15→0, y el resto a 0.
- `git diff --stat` = `git diff -w --stat` en los 14 archivos: sin sangría cambiada.
- Lógica sin clases contra la base de su rama: solo los formatos de montos permitidos (`formatUSD`/`formatVES` en subtotal, descuento, envío y total; `formatUSD` en el monto de Pago Móvil) y `type="button"` en `ErrorState`.
- Se mezcla con `main` (que ya tenía C-85 en el checkout) sin conflictos.
- `npx tsc --noEmit` 0 · `npm run build` OK · ESLint por archivo: ninguno empeora.

## Arreglos de la revisión (carril Claude, fuera de lo que pedía la tarjeta)
1. **Checkout:** `formatPrice` seguía devolviendo "USD 300,00$" (línea del producto en el resumen, saldo, faltante). Ahora usa `formatUSD`.
2. **`CartIcon`:**
   - El contador tenía estilos en línea (degradado con hex, letra de 9 px) que el grep de clases no ve, y cortaba en "9+" mientras la barra inferior mostraba "12". Ahora usa clases, es rojo como la campana y la barra inferior, y llega a "99+".
   - El menú del carrito tenía el mismo mapa viejo de diseños de gift card que C-86 arregló en `/carrito`: todas salían "aurora". Ahora usa `getGiftCardDesign`.
3. **`UserAccountButton`:** el menú de cuenta no se cerraba con Escape, solo con clic afuera.

## Verificación en el navegador (Firefox, tienda de ejemplo)
- Checkout con 12 unidades a 390 y 1440: ningún "USD …$"; montos `$300,00`, `$5,50`, `$305,50`, `Bs. 11.150,75`; contador "12" en el header y en la barra inferior; sin desborde.
- Menú de cuenta: abierto → Escape → cerrado, a 390 y 1440. Footer y menú con los tokens.
- `/admin/inquiries`, `/admin/product-requests` y `/admin/categories` (usan `Badge`, `StatusBadge`, `LoadingSpinner` y `Modal`) cargan sin error.
