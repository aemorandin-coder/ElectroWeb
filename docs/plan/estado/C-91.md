# C-91 — Revisión de `chatgpt/R1` (GPT-01, GPT-02), super merge y subida a GitHub
Estado: HECHO
Rama: claude/super-merge → `main` → `origin/main`
Pedido de Andrés (17/09): "Revisa a ChatGPT y Gemini antes del super merge, luego sube todo a GitHub para producción". Claude no edita código en esta tarea: revisa, mergea, sube y deja órdenes.

## Qué se revisó
- **Gemini:** `gemini/R13` no existe todavía (no empezó). R12 ya estaba en `main` (C-90). **Nada que mergear.**
- **ChatGPT:** `chatgpt/R1` con 2 commits (GPT-01 dashboard, GPT-02 órdenes y raspado de códigos del cliente). GPT-03 está a medias sin commit en su carpeta: **no entra en este merge**.

## Revisión de GPT-01 y GPT-02
- **Carril:** 7 archivos de código, todos del carril ChatGPT R1 más la ampliación que autorizó Andrés (`app/customer/(dashboard)/orders/[id]/digital/**`). Del layout del admin solo cambió el `className` permitido.
- **Lógica:** llamadas a la API, métodos, cuerpos, destinos (`href`, `router`), `localStorage` y copiado **idénticos** antes y después, en los 5 archivos. El filtro de estado pasó de `<select>` a botones con contador, con el mismo `setFilterStatus`; verificado en el navegador que "Pendientes" pide `/api/orders?status=PENDING`.
- **Raspado del cliente** (`PlatformScratchCard.tsx`, nuevo):
  - Revela al pasar 40 %, avisa a la página y la página conserva el guardado `revealed_order_<id>` y el copiado.
  - Agrega "Revelar sin raspar" (accesible sin arrastrar).
  - Colores de marca de cada plataforma en `.module.css`: excepción aceptada porque son identidades de terceros (Steam, PlayStation, Nintendo…). Los tokens de la tienda se usan en todo lo demás.
- **Sangría:** `git diff --stat` ≈ `git diff -w --stat` (377/365 y 281/259 en las dos páginas reescritas).
- `npx tsc --noEmit` 0. ESLint contra la base: dashboard 1→0, cliente digital 12→10, el resto igual; `PlatformScratchCard` 0. `npm run build` OK. Sin conflictos con `main`.
- **Navegador** (Firefox, tienda de ejemplo, 390 y 1440):
  - Dashboard con "Por atender" arriba, sin desborde.
  - Órdenes: 7 filtros, la acción siguiente visible también en el teléfono, detalle como modal que cubre la pantalla con notas. La entrega digital del admin carga.
  - Cliente: tarjeta de Steam, voltear y raspar con puntero real (102 movimientos) → "Código revelado".

## Pendiente para ChatGPT (queda en `PLAN_CHATGPT.md`)
- Órdenes en el teléfono: el ojo de "ver detalle" queda solo en una fila; "Actualizar" sigue como botón grande; las métricas se deslizan de lado sin indicio.
- `docs/plan/estado/GPT-02-preview.html` sobra en `estado/`.
- El commit de GPT-02 salió firmado "Gemini" (ya corregido en `CHATGPT.md`).
- **GPT-02b** "Cliente eliminado" en órdenes sin cliente (incidente del 17/09, `AUDITORIA_CLIENTES_BORRADOS.md`).

## Incidente de producción
Auditado en `docs/plan/AUDITORIA_CLIENTES_BORRADOS.md`: causa, consultas de diagnóstico (solo leen, probadas), pasos para Andrés desde el panel y tareas GPT-02b, G-52 y C-92.
