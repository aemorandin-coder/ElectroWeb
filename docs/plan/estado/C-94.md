# C-94 — Revisión de Gemini R16 y ChatGPT GPT-05/GPT-06, cierre de ChatGPT y plan final de Gemini
Estado: HECHO
Rama: `claude/C-94` (desde `main`, con `gemini/R16` y `chatgpt/R1` mezcladas). **Lista para mergear; no se subió.**

Pedido de Andrés (21/09):
1. Qué falta del plan maestro.
2. Revisar a Gemini y escribirle un mega prompt para que termine.
3. Revisar a ChatGPT y cerrar su trabajo: se va de vacaciones hasta nueva orden.

## Gemini `gemini/R16`: G-54, aprobada
- 3 archivos (`lib/email-service.ts`, `lib/email-templates/CourseCertificate.ts`, `app/api/orders/[id]/digital/route.ts`). `git diff` igual a `git diff -w`.
- Cumple la tarjeta:
  - Sin ⏳ ni ⏸.
  - Los 5 círculos vacíos borrados enteros; los que tienen texto (`OK`, `&#10003;`) se quedan.
  - El asunto de `sendCreatorStatusEmail` ya no empieza con espacio.
- Dos cosas de más, correctas y con el mismo criterio: el círculo ámbar de "Pedido en revisión", que quedaba vacío sin el ⏳, y el 🎮 del aviso de código digital.
- **Emojis en todo el código:** 0 (`app`, `components`, `lib`, `contexts`; rango completo más `FE0F`).
- **Arreglado aquí:** `platformColors` y `platformBg` de `sendDigitalCodeEmail` quedaron sin uso (solo los usaba el círculo). ESLint del archivo: 1 aviso → 0.

## ChatGPT `chatgpt/R1`: GPT-05 y GPT-06, aprobadas
Comparado contra `git merge-base main chatgpt/R1`: `main` avanzó y la comparación directa mezcla cambios ajenos. Ninguno de los 10 archivos cambió en `main` desde entonces: mezcla sin conflictos.
- **Llamadas:** `fetch`, `method`, `body`, `router` y `signIn`/`signOut` idénticos en los 10 archivos.
  - Enlaces nuevos solo a rutas que existen: `/recuperar-contrasena` en "Solicitar otro enlace" y `/login?verified=true` en "Ir al Login".
  - El `href="/"` y uno de `/login` pasan al "Volver" de `AuthShell`.
- **Lógica, comparada sin `className` ni sangría:**
  - **GPT-05:**
    - El menú de SVG a mano pasa a `react-icons`.
    - El cajón móvil se cierra solo al navegar: `drawerPath === pathname`, sin efecto que llame a `setState`.
    - Escape, `useBodyScrollLock` y `aria-expanded`/`aria-controls`.
    - Las flechas de texto pasan a `FiArrowLeft`/`FiArrowRight`/`FiExternalLink`.
    - Los montos ya usaban `formatUSD` (C-77).
  - **GPT-06:**
    - La contraseña nueva se valida en el navegador con `contrasenaSchema`, la misma regla del servidor. Antes pedía 6 caracteres y el servidor exigía otra cosa: mejora.
    - El estado "enlace vencido" sale de los mensajes reales de `/api/auth/reset-password` ("Token inválido o expirado", "El token ha expirado…", "Este enlace ya se usó…"). "Error al restablecer" sigue como error bajo el campo.
- **ESLint contra `main`:** ningún archivo empeora. Recuperar pasa de 1/1 a 0/0, restablecer de 0/1 a 0/0 y verificar de 0/1 a 0/0.
- **Navegador**, tienda de ejemplo, `next start` con el build de esta rama:

  | Prueba | Resultado |
  |---|---|
  | `/recuperar-contrasena`, `/recuperar-contrasena/<token>`, `/verificar-email/<token>` a 390 y 1440 | sin desborde |
  | Token falso: enviar una contraseña válida | "Enlace vencido o inválido" |
  | `/creator/dashboard` a 390, creadora `PENDING` | `main` de 390 px (antes ~138 px) |
  | Abrir el menú | `aria-expanded=true`, `html{overflow:hidden}`, cajón en x=0 |
  | Escape | se cierra |
  | Abrir y elegir "Mis Cursos" | navega a `/creator/dashboard/cursos` y el cajón queda cerrado |
  | Perfil a 390 | sin desborde |
  | `/creator/dashboard` a 1440 | menú lateral fijo, `main` de 1200 px |
- **Observaciones (no bloquean):**
  - Los dos commits salieron firmados "Gemini", por cuarta vez. No se reescriben: sería un rebase de una rama ajena.
  - El `aria-label` del avatar va sobre un `span` sin `role` y no se anuncia.
  - `verificar-email` conserva `prompt()` nativo.
  - Las capturas de GPT-05 quedaron en `/tmp`: se perdieron.

## Cierre de ChatGPT (vacaciones desde el 21/09)
- **Trabajo a medias sin commit** en `../ElectroShopVe-chatgpt`, rama `chatgpt/product-fixes-main`: arreglo de "el Home no se actualiza al guardar un producto" y "el wizard olvida el margen digital", pedido por Andrés después de GPT-06.
  - 9 archivos, 7 de ellos APIs y `lib` del carril Claude.
  - **Guardado tal cual** en el commit `386e56c` (firmado ChatGPT). La carpeta queda limpia.
- **Revisión del arreglo, para C-95:**
  - **Enfoque correcto.** `revalidatePath` después de cada escritura confirmada: crear, editar, archivar, borrar, masivos y SADES.
    - El último build confirma que, de las páginas que muestran productos, la única cacheada es `/`: ISR de 60 s, y el primero que entra después ve la versión vieja.
    - `/productos`, `/productos/[id]` y `/categorias/[category]` son dinámicas.
    - `revalidatePath` en Route Handlers está en la doc de Next 16 (`node_modules/next/dist/docs/…/revalidatePath.md`).
  - **Margen** en `specs.__adminPricing` sin migración: `toPublicSpecs` lo quita. Las rutas públicas leen `specs` solo por el DTO, y la ficha además descarta los objetos.
  - **Falla:** al editar un digital, el wizard manda `specifications` y el servidor reescribe `specs` entero. Se pierde `specs.digitalPricing`, el respaldo de `lib/order-quote.ts` para productos sin variantes en su tabla.
  - El ayudante está en `app/api/products/` y SADES lo importa desde ahí: va en `lib/`.
  - Sin `tsc`, build ni pruebas.
- **Pendiente de ChatGPT, redistribuido** (tabla en `PLAN_CHATGPT.md`, "Pausa del 21/09"):
  - GPT-02b → Gemini G-57.
  - Diálogos de `products/page.tsx` → G-58.
  - Tipos de las pantallas de R3 → G-59/G-60.
  - R2 y R3 de diseño esperan a ChatGPT. `products/_components/**` queda congelado hasta C-95.
- Borrado `docs/plan/estado/GPT-02-preview.html`: se le pidió a ChatGPT en C-91 y nunca se hizo.
- `CHATGPT.md` lleva el aviso de pausa arriba. `PLAN.md` §4 marca el carril en pausa.

## Verificación del conjunto (`claude/C-94`)
- `npx tsc --noEmit`: 0 errores.
- `npm run build` con `schema=rev10_demo`: OK (la base local `public` no tiene las columnas nuevas).
- ESLint por archivo contra `main`, en los 13 archivos de código que cambian: ninguno empeora. Tres bajan: recuperar, restablecer y verificar.

## Inventario al 21/09 (base del plan final de Gemini)
**ESLint de `app components lib contexts proxy.ts`: 201 errores y 117 avisos.**

| Carril | Errores | Avisos | Archivos |
|---|---|---|---|
| Gemini | 31 | 4 | 19 |
| Pantallas de ChatGPT (en pausa) | 100 | 86 | 25 |
| Claude | 58 | 22 | 18 |
| Admin de Claude (`cursos`, `layout`) | 8 | 2 | 2 |
| `components/admin` | 4 | 3 | 2 |

- **Por regla:** `no-explicit-any` 107, `no-unused-vars` 91, `set-state-in-effect` 41, `immutability` 34, `exhaustive-deps` 18, `purity` 9 y otras 18.
- **`immutability` son los 34 "Cannot access variable before it is declared":** un `useEffect` que llama a un `const f = async () => {}` declarado más abajo.
  - Pasar la función a `async function f() {}` lo resuelve sin mover código ni cambiar cuándo corre: probado con ESLint en `addresses/page.tsx`.
  - Es la receta de G-56 y G-60.
- **Patrones prohibidos que quedan en el carril de Gemini:**
  - 4 montos con `toFixed` (G-55).
  - 1 flecha de texto (G-55).
  - Los 6 `confirm(` son `useConfirm`, no nativos.
- **En las pantallas de ChatGPT:**
  - 7 `alert` y 1 `confirm` nativos, todos en `products/page.tsx` (G-58), y 1 `prompt` en `verificar-email`.
  - Los hex son colores de marcas externas en `payments` (PayPal, Binance, Zelle…).
  - `ProductForm.tsx`, sin uso, acumula 33 `indigo/purple` y 6 `toFixed`: se borra en GPT-11.

## Plan final de Gemini (`PLAN_GEMINI.md`, R17-R19)
- **R17, cierre de su carril:**
  - G-55: montos con `formatUSD`/`formatVES`, flecha → ícono, ternario suelto, y `/creator` fuera del recorrido (pedido de GPT-05).
  - G-56: 13 `immutability`.
- **R18, pantallas del admin sin dueño:**
  - G-57 "Cliente eliminado".
  - G-58, diálogos nativos de productos.
  - G-59, 102 problemas de tipos y sin uso en 18 archivos.
  - G-60, 13 `immutability`.
- **R19, G-61:** inventario final de solo lectura y recorrido visual, para C-40.
- `GEMINI.md` §2 tiene el carril exacto de cada ronda. El mega prompt está en `SIGUIENTE.md` §5.

## Qué falta del plan maestro
- **Claude:**
  - C-95, el arreglo de productos de ChatGPT.
  - C-92, desactivar clientes (espera el diagnóstico de Andrés y su OK para la migración).
  - C-80: límite de intentos de login, cuentas desactivadas, DTO de `/api/user/profile` y `/api/debug/og-metadata`.
  - C-60b, surtido de pedidos digitales.
  - Facebook, cuando exista la app en Meta.
  - Revisar Gemini R17-R19.
  - C-40, cierre: README y checklist de `PLAN.md` §6.
- **Gemini:** R17-R19, sus últimas rondas.
- **ChatGPT (en pausa):** pendientes de GPT-02, R2 (productos, GPT-07…GPT-11) y R3 (resto del admin, GPT-12…GPT-16).
- **Andrés:**
  - Mergear y desplegar C-94.
  - Diagnóstico de clientes borrados.
  - Claves de Google cuando quiera activarlo.
  - Borrar el esquema `rev10_demo` cuando termine el ciclo de pruebas.
- **Fases 0 a 3 del plan maestro** (C-01…C-33, G-01…G-54): todas `HECHO`, salvo el cierre C-40. Ningún estado dice `BLOQUEADO`.

## Encontrado al pasar (anotado en `PLAN_CLAUDE.md`, fila 13c)
- **Cajones móviles del admin, del cliente (C-55) y del creador:** cerrados siguen en el DOM fuera de pantalla, y con Tab el foco entra en sus enlaces. Arreglo: `inert` cuando está cerrado en el teléfono. Carril Claude.
- **Reglas de hooks que no son mecánicas**, fuera de Gemini: `set-state-in-effect` 41, `purity` 9 y `exhaustive-deps` 18.
- **`any` en archivos sensibles**, fuera de Gemini desde R14: `lib/auth.ts` 14 y `app/checkout/page.tsx` 9.
- `ImageUploadField` ilegible sobre fondo claro: pedido de GPT-05, `components/ui`.
