# C-98 — Revisión de Gemini R17-R19 (G-55…G-61)
Estado: HECHO
Rama: `claude/C-98` = `claude/C-97` + `gemini/R19` (trae R17 y R18), sin conflictos.

## Veredicto: aprobadas las 7 tarjetas
- **Carril:** los 34 archivos de código están dentro de lo autorizado para R17-R19. `products/_components/**` no se tocó.
- **Llamadas:** `fetch`, `method`, `body`, `href`, `router` y `signOut` **idénticos** en los 34 archivos, contados antes y después contra `claude/C-94`, de donde partió.
- **Sin reindentar:** `git diff` igual a `git diff -w` en todos (en R10-R11 había sido 1.363 líneas contra 231).
- `'use client'` sigue siendo la primera línea en todos.
- **Commits** firmados "Gemini" y con su estado.
- `tsc` 0 y `npm run build` OK sobre la mezcla con C-95…C-97.
- **Estados honestos:** G-56 y G-60 anotan qué errores quedaron y por qué, en vez de mover código para taparlos (lo prohibía la tarjeta).

| Tarjeta | Revisión |
|---|---|
| G-55 | Exacta a la tarjeta: 4 montos con `formatUSD`/`formatVES`, flecha → `FiArrowRight`, ternario → `if`, `/creator` fuera del recorrido. |
| G-56 / G-60 | 26 funciones `const … => {` → `function`. **Comprobado con el AST de TypeScript** que cada `};` → `}` cambiado es el cierre de su función (un cierre fuera de lugar también compilaría). 26 de 26 bien. |
| G-57 | "Cliente eliminado" (badge neutral + `FiUserX`) en la tarjeta y el detalle; "Invitado" con su correo si hay `guestEmail`. No hay otro lugar que muestre al cliente. |
| G-58 | 7 `alert` → `toast` y el `confirm` del borrado masivo → `useConfirm`, con los mismos textos. En un segundo commit encontró y cambió `window.confirm` de la sincronización con SADES, que el grep de la tarjeta no atrapaba (el punto lo excluía): buena iniciativa, mismo texto. |
| G-59 | Tipos, imports y variables sin uso, sin cambiar lógica. Al tipar arregló un fallo real: el badge "Rechazada" de consultas y solicitudes usaba `variant: 'error'`, que `Badge` no tiene, y salía sin color. |
| G-61 | Inventario de solo lectura para C-40. El conteo de "alert/confirm nativos" incluye los `confirm({` de `useConfirm`: hay que leerlo con eso en mente. Recorrido visual: `QA visual pendiente` (lo hizo Claude, abajo). |

**Una sola falta de proceso:** hizo `git merge gemini/R18` dentro de `gemini/R19` para llevar el segundo commit de G-58. Son sus propias ramas y no rompe nada, pero el plan decía sin merge.

## ESLint del proyecto (`app components lib contexts proxy.ts`)
- **201 → 144 errores, 117 → 52 avisos** (medido el 21/09 en C-94 y ahora).
- Pantallas del admin:

  | Pantalla | Antes | Después |
  |---|---|---|
  | `payments` | 13/15 | 0/1 |
  | `discount-requests` | 4/9 | 0/1 |
  | `verifications` | 1/8 | 0/1 |
  | `orders/[id]/digital` | 2/8 | 0/1 |
  | `customers` | 6/1 | 0/1 |
  | `orders` | 7/4 | 3/1 |

  Ningún archivo empeora.
- **La receta de G-56/G-60 era mía y solo resolvió 16 de 26:** en 10 funciones la regla `react-hooks/immutability` de React sigue marcando aunque ya sean `function`.
  - En ejecución no hay fallo: la función existe cuando el efecto corre.
  - Para dejarla en 0 hay que mover el `useEffect` debajo de la función. Queda en la fila 13c de `PLAN_CLAUDE.md`.

## Recorrido visual (Claude, tienda de ejemplo)
- **Órdenes**, con una orden sin cliente y otra de invitado creadas para la prueba:
  - A 390 y 1440 px: "Cliente eliminado" con su badge; la de invitado dice "Invitado".
  - Sin desborde.
  - El detalle a 1440 muestra "Cliente eliminado".
- **Productos → SADES → "Sincronizar Todo Ahora":** abre el diálogo de la tienda "Iniciar sincronización"; "Cancelar" lo cierra y no sincroniza.
- Las órdenes de prueba se borraron; el producto de ejemplo quedó intacto.

## Encontrado al pasar
1. **Las acciones masivas de productos no se pueden abrir desde la pantalla.**
   - `handleSelectAll` y `handleSelectProduct` existen, pero ninguna casilla las llama: se perdieron en febrero (`dc6ca41`, antes de este plan).
   - La barra "N seleccionados" (editar precio, % o stock de varios y borrar varios) nunca aparece.
   - La API de esas acciones está validada (C-97), pero no hay cómo usarla. Es de la pantalla de productos (GPT-08).
2. **`ConfirmDialog`** (`components/modals`, carril Gemini) usa `whitespace-normal`: el mensaje de SADES con viñetas sale en una sola línea. Arreglo: `whitespace-pre-line`.
3. De C-80 sigue pendiente para Gemini: el perfil no copia `businessVerificationNotes` ("Motivo del rechazo" nunca se ve).

## Para una próxima orden de Gemini (no hay ronda abierta)
- `components/modals/ConfirmDialog.tsx`: `whitespace-normal` → `whitespace-pre-line`.
- `app/customer/(dashboard)/profile/page.tsx`: `businessVerificationNotes: data.profile?.businessVerificationNotes || ''` en `profileData`.
