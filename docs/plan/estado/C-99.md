# C-99 — Salida de ChatGPT, ronda R20 de Gemini y auditoría de envíos
Estado: HECHO
Rama: `claude/C-99` (desde `main`). **Solo documentos: no cambia código ni pruebas.** Lista para mergear; no se subió.

Pedido de Andrés (21/09, ya en producción):
1. Qué le falta a Gemini y su prompt.
2. Revisar las compras de digitales y físicos, y si el flujo de envío es lógico, también desde el panel. Averiguar API o script para ZOOM y MRW, para que el cliente gestione y rastree su envío.
3. Sin mover código ni pruebas. Sacar a ChatGPT del equipo.
4. Qué queda de rediseño.

## Qué cambió
- **ChatGPT fuera del equipo:** `CLAUDE.md` (dos agentes, reglas 6 y 7), `PLAN.md` §4, `GEMINI.md`, `CHATGPT.md` y `PLAN_CHATGPT.md` quedan como historial, con la tabla "Salida del 21/09" que reparte lo pendiente.
- **Gemini R20** en `PLAN_GEMINI.md` (G-62…G-66) y su prompt en `SIGUIENTE.md` §5.
- **`AUDITORIA_ENVIOS.md`** (nuevo): cómo funciona hoy, 14 hallazgos, API de ZOOM probada, situación de MRW, propuesta C-100 y 4 decisiones.
- `PLAN_CLAUDE.md`: filas 13d y 15-18. `SIGUIENTE.md`: ramas, limpieza de ChatGPT y prompts.

## Cómo se verificó
- Los 14 hallazgos de envíos se leyeron en el código con archivo y línea. E4 (enviar sin pago) y E13 (digital sin cerrar) se confirmaron buscando cualquier otra guarda o `order.update`: no hay.
- **ZOOM**, servidor de pruebas, 21/09: `getEstados`, `getCiudades`, `getOficinasGE`, `CalcularTarifa` (oficina, domicilio y cobro a destino) y `getInfoTracking` responden sin credenciales. Resultados en `AUDITORIA_ENVIOS.md` §3.
- **MRW**: `www.mrw.com.ve` no resuelve (el enlace del panel está muerto). `mrwve.com/api/agencias` devuelve 250 agencias. `mrwve.com/api/tracking` respondía "actualizando nuestro sistema de rastreo".
- **Tarjetas de R20:** líneas y conteos sacados de ESLint del 21/09 (145 errores, 52 avisos en `app components lib contexts proxy.ts`). `git grep ProductForm` solo encuentra el propio archivo. `window.confirm` queda 1 en todo el proyecto (gift cards).
- Las ramas de ChatGPT no tienen nada fuera de `main`, salvo `386e56c`, que C-95 reemplazó.

## Revisión de `[Marketing]` (Gemini, 2 commits directos en `main`)
- `f09aaf3` y `d1e1cc7`: botón "Ver correos" con modal de destinatarios, logo en la cabecera de los correos e íconos PNG de las redes.
- **Sin bloqueo:** el modal usa las recetas, `useBodyScrollLock` y Escape. El segundo commit arregla la expresión de WhatsApp que rompió el primero (`\\D` dentro de la plantilla).
- **Queda para Claude (fila 18):** `GET /api/admin/campaigns/recipients` devuelve todos los correos, sin límite, a cualquiera con `MANAGE_CONTENT`.
- **Proceso:** entraron a `main` y a producción sin rama ni revisión, y tocan el carril de Claude (`app/api`, `lib/email-service.ts`). El prompt de R20 lo prohíbe.

## Qué queda
- **Andrés:** decisiones D-E1…D-E4 (`AUDITORIA_ENVIOS.md` §6), mergear `claude/C-99`, lanzar a Gemini con el prompt de §5 y, cuando quiera, borrar la carpeta y las ramas de ChatGPT (comandos en `SIGUIENTE.md` §1).
- **Claude:** C-100 (envíos), C-51 (productos) después de R20, rediseño del resto del panel, fila 18, y lo que ya esperaba (C-92, 13c, C-40).
