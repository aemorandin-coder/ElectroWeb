# G-25
Estado: HECHO
Rama: gemini/R7
- Archivos: 9
- Verificación:
  - grep `<style jsx` en app/customer app/gift-cards components/modals components/orders app/admin/(dashboard)/reports: 0 resultados
  - grep animaciones infinitas/no estándar en app/customer app/gift-cards components/orders: 0 resultados
- tsc: sin errores nuevos
- Cambios realizados:
  1. `components/modals/ConfirmDialog.tsx`: eliminado bloque `<style jsx>`
  2. `components/modals/BalanceTermsModal.tsx`: eliminado bloque `<style jsx>`
  3. `app/customer/(dashboard)/wishlist/page.tsx`: eliminado bloque `<style jsx>`
  4. `app/admin/(dashboard)/reports/page.tsx`: eliminado bloque `<style jsx>`
  5. `app/customer/(dashboard)/layout.tsx`: eliminado bloque `<style jsx>`
  6. `app/customer/(dashboard)/profile/page.tsx`: reemplazada animación modalScaleIn en style por clase `animate-scaleIn` y eliminado `<style jsx>`
  7. `app/gift-cards/page.tsx`: eliminadas propiedades de animación infinita en style y bloque `<style jsx global>`
  8. `app/customer/(dashboard)/orders/page.tsx`: eliminadas clases `animate-spin-slow` y `animate-bounce-subtle` y bloque `<style jsx>`
  9. `components/orders/OrderTracking.tsx`: eliminadas clases `animate-epic*`, `animate-iconBounce`, `animate-checkDraw`, `animate-truck`, eliminados elementos que solo existían para la animación y bloque `<style jsx>`
