# G-26
Estado: HECHO
Rama: gemini/R7
- Archivos: 15
- Verificación:
  - git diff HEAD con formatUSD/formatVES: 45 llamadas agregadas (igual a las 45 filas de la tabla)
  - git diff HEAD en body, JSON.stringify o clipboard: 0 resultados
- tsc: sin errores nuevos

### Tabla de reemplazos (45):
| Archivo | Línea | Antes | Después |
|---|---|---|---|
| `app/canjear-gift-card/page.tsx` | 95 | `toast.success(`¡$${data.amountRedeemed.toFixed(2)} agregados a tu saldo!`);` | `toast.success(`¡${formatUSD(data.amountRedeemed)} agregados a tu saldo!`);` |
| `app/canjear-gift-card/page.tsx` | 119 | `${redeemedAmount.toFixed(2)}` | `{formatUSD(redeemedAmount)}` |
| `app/canjear-gift-card/page.tsx` | 213 | `${Number(cardInfo.balanceUSD).toFixed(2)}` | `{formatUSD(Number(cardInfo.balanceUSD))}` |
| `app/canjear-gift-card/page.tsx` | 259 | `{isLoading ? '...' : <><HiSparkles /> Canjear ${Number(cardInfo.balanceUSD).toFixed(2)}</>}` | `{isLoading ? '...' : <><HiSparkles /> Canjear {formatUSD(Number(cardInfo.balanceUSD))}</>}` |
| `app/creator/dashboard/cursos/nuevo/page.tsx` | 134 | `<p className="text-white/80 text-xs mt-1">Recibirás el 90% de cada venta (${form.priceUSD ? (parseFloat(form.priceUSD) * 0.9).toFixed(2) : '0.00'} por venta)</p>` | `<p className="text-white/80 text-xs mt-1">Recibirás el 90% de cada venta ({formatUSD(form.priceUSD ? parseFloat(form.priceUSD) * 0.9 : 0)} por venta)</p>` |
| `app/creator/dashboard/cursos/page.tsx` | 112 | `<span className="text-white/80 font-bold">${course.priceUSD.toFixed(2)}</span>` | `<span className="text-white/80 font-bold">{formatUSD(course.priceUSD)}</span>` |
| `app/creator/dashboard/page.tsx` | 87 | `{ label: 'Ingresos', value: `$${creator.totalRevenue.toFixed(2)}`, sub: `${creator.commissionRate}% tuyo por venta`, color: 'from-emerald-500/20 to-emerald-600/5' },` | `{ label: 'Ingresos', value: formatUSD(creator.totalRevenue), sub: `${creator.commissionRate}% tuyo por venta`, color: 'from-emerald-500/20 to-emerald-600/5' },` |
| `app/creator/dashboard/page.tsx` | 163 | `<span className="text-white/80 font-semibold">${course.priceUSD.toFixed(2)}</span>` | `<span className="text-white/80 font-semibold">{formatUSD(course.priceUSD)}</span>` |
| `app/cursos/page.tsx` | 175 | `{price === 0 ? 'Gratis' : `$${price.toFixed(2)}`}` | `{price === 0 ? 'Gratis' : formatUSD(price)}` |
| `app/customer/(dashboard)/balance/page.tsx` | 183 | `${userBalance?.balance.toFixed(2) \|\| '0.00'}` | `{formatUSD(userBalance?.balance \|\| 0)}` |
| `app/customer/(dashboard)/balance/page.tsx` | 351 | `${userBalance?.balance.toFixed(2) \|\| '0.00'}` | `{formatUSD(userBalance?.balance \|\| 0)}` |
| `app/customer/(dashboard)/balance/page.tsx` | 449 | `{transaction.type === 'RECHARGE' ? '+' : '-'}${transaction.amount.toFixed(2)}` | `{transaction.type === 'RECHARGE' ? '+' : '-'}{formatUSD(transaction.amount)}` |
| `app/customer/(dashboard)/orders/page.tsx` | 647 | `<p className="text-xs font-bold text-brand-500">${item.totalUSD.toFixed(2)}</p>` | `<p className="text-xs font-bold text-brand-500">{formatUSD(item.totalUSD)}</p>` |
| `app/customer/(dashboard)/orders/page.tsx` | 661 | `<p className="text-base font-bold">USD {selectedOrder.totalUSD.toFixed(2)}</p>` | `<p className="text-base font-bold">{formatUSD(selectedOrder.totalUSD)}</p>` |
| `app/customer/(dashboard)/page.tsx` | 129 | `<p className="text-base lg:text-xl font-bold">${stats?.balance?.toFixed(2) \|\| '0.00'}</p>` | `<p className="text-base lg:text-xl font-bold">{formatUSD(stats?.balance \|\| 0)}</p>` |
| `app/customer/(dashboard)/page.tsx` | 175 | `<p className="font-bold text-ink text-sm lg:text-base">${order.total.toFixed(2)}</p>` | `<p className="font-bold text-ink text-sm lg:text-base">{formatUSD(order.total)}</p>` |
| `app/customer/(dashboard)/page.tsx` | 249 | `{activity.type === 'RECHARGE' \|\| activity.type === 'DEPOSIT' ? '+' : activity.type === 'PURCHASE' ? '-' : ''}${activity.amount?.toFixed(2) \|\| ''}` | `{activity.type === 'RECHARGE' \|\| activity.type === 'DEPOSIT' ? '+' : activity.type === 'PURCHASE' ? '-' : ''}{formatUSD(activity.amount \|\| 0)}` |
| `app/customer/(dashboard)/page.tsx` | 274 | `<span className="font-bold text-green-600 text-xs lg:text-sm">${stats?.totalRecharges?.toFixed(2) \|\| '0.00'}</span>` | `<span className="font-bold text-green-600 text-xs lg:text-sm">{formatUSD(stats?.totalRecharges \|\| 0)}</span>` |
| `app/customer/(dashboard)/page.tsx` | 278 | `<span className="font-bold text-purple-600 text-xs lg:text-sm">${stats?.totalSpent?.toFixed(2) \|\| '0.00'}</span>` | `<span className="font-bold text-purple-600 text-xs lg:text-sm">{formatUSD(stats?.totalSpent \|\| 0)}</span>` |
| `app/customer/(dashboard)/page.tsx` | 282 | `<span className="font-bold text-brand-500 text-xs lg:text-sm">${stats?.totalSpentThisMonth?.toFixed(2) \|\| '0.00'}</span>` | `<span className="font-bold text-brand-500 text-xs lg:text-sm">{formatUSD(stats?.totalSpentThisMonth \|\| 0)}</span>` |
| `app/customer/(dashboard)/referrals/page.tsx` | 396 | `value={`$${stats.pendingEarnings.toFixed(2)}`}` | `value={formatUSD(stats.pendingEarnings)}` |
| `app/customer/(dashboard)/referrals/page.tsx` | 403 | `value={`$${stats.approvedEarnings.toFixed(2)}`}` | `value={formatUSD(stats.approvedEarnings)}` |
| `app/customer/(dashboard)/referrals/page.tsx` | 410 | `value={`$${stats.thisMonthEarnings.toFixed(2)}`}` | `value={formatUSD(stats.thisMonthEarnings)}` |
| `app/customer/(dashboard)/referrals/page.tsx` | 660 | `${conv.commission.toFixed(2)}` | `{formatUSD(conv.commission)}` |
| `app/customer/(dashboard)/referrals/page.tsx` | 736 | `${entry.totalEarnings.toFixed(2)}` | `{formatUSD(entry.totalEarnings)}` |
| `app/customer/(dashboard)/wishlist/page.tsx` | 448 | `<span className="text-xs text-muted line-through">${item.price.toFixed(2)}</span>` | `<span className="text-xs text-muted line-through">{formatUSD(item.price)}</span>` |
| `app/customer/(dashboard)/wishlist/page.tsx` | 450 | `${(item.price * (1 - (discountStatus?.approvedDiscount \|\| 0) / 100)).toFixed(2)}` | `{formatUSD(item.price * (1 - (discountStatus?.approvedDiscount \|\| 0) / 100))}` |
| `app/customer/(dashboard)/wishlist/page.tsx` | 454 | `<span className="text-base font-bold text-brand-500">${item.price.toFixed(2)}</span>` | `<span className="text-base font-bold text-brand-500">{formatUSD(item.price)}</span>` |
| `app/customer/(dashboard)/wishlist/page.tsx` | 517 | `<p className="text-xl font-bold text-brand-500">${item.price.toFixed(2)}</p>` | `<p className="text-xl font-bold text-brand-500">{formatUSD(item.price)}</p>` |
| `app/customer/(dashboard)/wishlist/page.tsx` | 628 | `<p className="text-2xl font-bold text-brand-500 mt-1">${selectedItem.price.toFixed(2)}</p>` | `<p className="text-2xl font-bold text-brand-500 mt-1">{formatUSD(selectedItem.price)}</p>` |
| `app/customer/(dashboard)/wishlist/page.tsx` | 657 | `<span className="font-semibold text-ink">${selectedItem.price.toFixed(2)}</span>` | `<span className="font-semibold text-ink">{formatUSD(selectedItem.price)}</span>` |
| `app/customer/(dashboard)/wishlist/page.tsx` | 661 | `<span className="font-semibold text-blue-600">-${(selectedItem.price * discountPercent / 100).toFixed(2)}</span>` | `<span className="font-semibold text-blue-600">-{formatUSD(selectedItem.price * discountPercent / 100)}</span>` |
| `app/customer/(dashboard)/wishlist/page.tsx` | 665 | `<span className="text-2xl font-bold text-emerald-600">${(selectedItem.price * (1 - discountPercent / 100)).toFixed(2)}</span>` | `<span className="text-2xl font-bold text-emerald-600">{formatUSD(selectedItem.price * (1 - discountPercent / 100))}</span>` |
| `app/gift-cards/page.tsx` | 1088 | `<span className="font-bold text-gray-900">${finalAmount.toFixed(2)}</span>` | `<span className="font-bold text-gray-900">{formatUSD(finalAmount)}</span>` |
| `app/gift-cards/page.tsx` | 1098 | `${(typeof userBalance === 'number' ? userBalance : 0).toFixed(2)}` | `{formatUSD(typeof userBalance === 'number' ? userBalance : 0)}` |
| `app/gift-cards/page.tsx` | 1212 | `¿Quieres enviarle una invitación para que cree su cuenta y pueda recibir tu regalo de <strong>${finalAmount.toFixed(2)}</strong>?` | `¿Quieres enviarle una invitación para que cree su cuenta y pueda recibir tu regalo de <strong>{formatUSD(finalAmount)}</strong>?` |
| `components/cursos/CourseDetailClient.tsx` | 490 | `{isFree ? 'Gratis' : `$${price.toFixed(2)}`}` | `{isFree ? 'Gratis' : formatUSD(price)}` |
| `components/cursos/CourseDetailClient.tsx` | 511 | `Tu saldo: <span className={`font-bold ${hasBalance ? 'text-green-600' : 'text-red-500'}`}>${userBalance.toFixed(2)}</span>` | `Tu saldo: <span className={`font-bold ${hasBalance ? 'text-green-600' : 'text-red-500'}`}>{formatUSD(userBalance)}</span>` |
| `components/modals/RechargeModalV2.tsx` | 286 | `message: `¿Estas seguro de que deseas recargar $${parseFloat(amount).toFixed(2)} usando ${companyPaymentMethods.find(m => m.type === selectedMethod)?.name}?`,` | `message: `¿Estas seguro de que deseas recargar ${formatUSD(parseFloat(amount))} usando ${companyPaymentMethods.find(m => m.type === selectedMethod)?.name}?`,` |
| `components/modals/RechargeModalV2.tsx` | 604 | `<span className="text-muted">USD ${amount ? parseFloat(amount).toFixed(2) : '0.00'}</span>` | `<span className="text-muted">{formatUSD(amount ? parseFloat(amount) : 0)}</span>` |
| `components/modals/RechargeModalV2.tsx` | 610 | `Bs. {amountInBs ? amountInBs.toFixed(2) : '0.00'}` | `{formatVES(amountInBs \|\| 0)}` |
| `components/pago-movil/VerificarPagoMovilForm.tsx` | 490 | `<p className="text-xs text-muted">(${montoEsperado.toFixed(2)} USD)</p>` | `<span className="text-2xl font-bold text-orange-600">{formatVES(montoEnBs)}</span>` |
| `components/pago-movil/VerificarPagoMovilForm.tsx` | 490 | `` | `<p className="text-xs text-muted">({formatUSD(montoEsperado)})</p>` |
| `components/pago-movil/VerificarPagoMovilForm.tsx` | 494 | `<span className="text-xl font-bold text-brand-500">${montoEsperado.toFixed(2)}</span>` | `<span className="text-xl font-bold text-brand-500">{formatUSD(montoEsperado)}</span>` |
| `components/social/ShareEarnModal.tsx` | 186 | `Gana <strong className="font-bold text-green-800">{commissionRate}%</strong> de comisión (${estimatedEarnings.toFixed(2)} USD) si alguien compra este artículo a través de tu enlace.` | `Gana <strong className="font-bold text-green-800">{commissionRate}%</strong> de comisión ({formatUSD(estimatedEarnings)}) si alguien compra este artículo a través de tu enlace.` |

### Montos dejados sin modificar (25):
- **toFixed(0)** (12): cantidades enteras o sin decimales:
  - `app/customer/(dashboard)/balance/page.tsx:196`: `totalRecharges.toFixed(0)`
  - `app/customer/(dashboard)/balance/page.tsx:205`: `totalSpent.toFixed(0)`
  - `app/customer/(dashboard)/balance/page.tsx:288`: `transaction.amount.toFixed(0)`
  - `app/customer/(dashboard)/balance/page.tsx:360`: `totalRecharges.toFixed(0)`
  - `app/customer/(dashboard)/balance/page.tsx:370`: `totalSpent.toFixed(0)`
  - `app/customer/(dashboard)/orders/page.tsx:243`: `stats.totalSpent.toFixed(0)`
  - `app/customer/(dashboard)/orders/page.tsx:380`: `order.totalUSD.toFixed(0)`
  - `app/customer/(dashboard)/orders/page.tsx:420`: `stats.totalSpent.toFixed(0)`
  - `app/customer/(dashboard)/orders/page.tsx:538`: `order.totalUSD.toFixed(0)`
  - `app/customer/(dashboard)/profile/page.tsx:1084`: `stats.totalSpent.toFixed(0)`
  - `app/customer/(dashboard)/wishlist/page.tsx:293`: `totalValue.toFixed(0)`
  - `app/gift-cards/page.tsx:677`: `finalAmount.toFixed(0)`
- **toFixed(1)** (9): ratings y tamaños no monetarios:
  - `components/customer/DocumentUpload.tsx:118`: tamaño de archivo en KB
  - `app/creator/dashboard/page.tsx:88`: rating promedio
  - `app/creator/dashboard/page.tsx:160`: rating del curso
  - `app/creator/dashboard/cursos/page.tsx:108`: rating del curso
  - `app/cursos/page.tsx:159`: rating del curso
  - `components/cursos/CourseDetailClient.tsx:176`: rating del curso
  - `components/servicios/ServiciosPortfolio.tsx:255`: rating promedio
  - `components/reviews/ReviewStats.tsx:37`: rating promedio
  - `components/reviews/StarRating.tsx:61`: rating
- **Tasa / multiplicador de cambio** (1):
  - `components/modals/RechargeModalV2.tsx:604`: `exchangeRate.toFixed(2)` (factor multiplicador 'x 36.50')
- **Mensaje para WhatsApp / API / Contacto** (3):
  - `components/social/ShareEarnModal.tsx:119`: mensaje preformateado para URL de WhatsApp (`wa.me/?text=...`)
  - `components/pago-movil/VerificarPagoMovilForm.tsx:132-133`: texto formateado para parámetro de URL / mensaje de contacto de soporte
