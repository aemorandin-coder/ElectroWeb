# G-29
Estado: HECHO
Rama: gemini/R8
- Archivos: 6
  - `app/admin/(dashboard)/products/_components/wizard/physical/Step1BasicInfo.tsx` (18 clases migradas a `wizard/ui.ts` y tokens)
  - `app/admin/(dashboard)/products/_components/wizard/physical/Step2Prices.tsx` (28 clases migradas a `wizard/ui.ts` y tokens)
  - `app/admin/(dashboard)/products/_components/wizard/physical/Step3Specs.tsx` (15 clases migradas a `wizard/ui.ts` y tokens)
  - `app/admin/(dashboard)/products/_components/wizard/StepSEO.tsx` (13 clases migradas a `wizard/ui.ts` y tokens)
  - `app/admin/(dashboard)/products/_components/wizard/ImagePanel.tsx` (16 clases migradas a tokens de la tienda)
  - `app/admin/(dashboard)/products/_components/wizard/SadesSearchModal.tsx` (15 clases migradas a tokens y capa modal)

- Verificación:
  1. `git diff --stat main`:
     ```
     ImagePanel.tsx     |  56 +++++-----
     SadesSearchModal.tsx |  42 +++----
     StepSEO.tsx        |  37 ++++---
     Step1BasicInfo.tsx |  60 +++++-----
     Step2Prices.tsx    | 121 ++++++++++-----------
     Step3Specs.tsx     |  45 ++++----
     6 files changed, 172 insertions(+), 189 deletions(-)
     ```
  2. `grep -nE "(gray|blue|red|green|amber|yellow)-[0-9]|bg-black|font-black|z-50"`: 0 resultados.
  3. `grep -n "\[#"`: solo las 3 clases del preview Google en `StepSEO.tsx` (`text-[#1a0dab]`, `text-[#006621]`, `text-[#4d5156]`).
  4. `git diff main -- "$W" | grep -E "^[-+]" | grep -vE "className|^\+\+\+|^---|import .* from '\.\.?/ui'"`: 37 líneas que corresponden exclusivamente a líneas dentro de expresiones multilínea `className={[ ... ].join(' ')}` que existían en `main`. Ninguna línea de lógica, props, JSX o texto fue modificada.
  5. `npx tsc --noEmit`: 0 errores.

- Clases conservadas por no estar en la tabla o ser intencionales:
  - `text-[#1a0dab]`, `text-[#006621]`, `text-[#4d5156]` en `StepSEO.tsx` (simulación intencional de Google Snippet).
  - Clases de layout/posicionamiento: `pl-8`, `pr-12`, `font-mono`, `w-48`, etc. (agregadas a `wizardInput`).
  - Capas modales: `z-[var(--z-modal)]` y `bg-ink/50 backdrop-blur-sm` en `SadesSearchModal.tsx`.
