# G-38
Estado: HECHO
Rama: gemini/R10
- Archivos: 6
- Verificación: 0 colores sueltos, 0 clases malas, 0 imágenes fill sin sizes, tsc sin errores nuevos en los 6 archivos
- tsc: sin errores nuevos en los archivos de la tarea

### Tabla de Archivos y Métricas

| Archivo | Colores sueltos (antes → después) | Modales con `useBodyScrollLock` | Tablas envueltas | `toFixed` cambiados |
|---------|-----------------------------------|---------------------------------|------------------|---------------------|
| `app/customer/(dashboard)/page.tsx` | 49 → 0 | 0 | 0 | 0 |
| `app/customer/(dashboard)/orders/page.tsx` | 96 → 0 | 1 | 0 | 4 |
| `app/customer/(dashboard)/orders/[id]/digital/page.tsx` | 64 → 0 | 0 | 0 | 0 |
| `app/customer/(dashboard)/balance/page.tsx` | 63 → 0 | 0 | 0 | 5 |
| `components/modals/RechargeModalV2.tsx` | 31 → 0 | 1 | 0 | 0 |
| `components/orders/OrderTracking.tsx` | 31 → 0 | 0 | 0 | 0 |

Total colores sueltos: 334 → 0
Total clases prohibidas / degradados eliminados: 89 → 0

### Salida de la verificación

```bash
# 1. Colores sueltos en los 6 archivos: 0
TOTAL LOOSE: 0

# 2. Clases prohibidas (z-40/50, bg-gradient-to, backdrop-blur, animate-bounce/ping, font-black/extrabold, text-[10px], hex): 0
TOTAL BAD: 0

# 3. Imágenes fill sin sizes: 0
Total fill without sizes: 0

# 4. Modales y scroll lock:
app/customer/(dashboard)/orders/page.tsx: fixed inset-0 = 1, useBodyScrollLock = 1
components/modals/RechargeModalV2.tsx: fixed inset-0 = 1, useBodyScrollLock = 1

# 5. 'use client' en línea 1:
app/customer/(dashboard)/page.tsx: line 1 is 'use client';
app/customer/(dashboard)/orders/page.tsx: line 1 is 'use client';
app/customer/(dashboard)/orders/[id]/digital/page.tsx: line 1 is 'use client';
app/customer/(dashboard)/balance/page.tsx: line 1 is 'use client';
components/modals/RechargeModalV2.tsx: line 1 is 'use client';
components/orders/OrderTracking.tsx: line 1 is 'use client';

# 6. Sangría (git diff --stat vs git diff -w --stat):
6 files changed, 413 insertions(+), 513 deletions(-)
6 files changed, 367 insertions(+), 467 deletions(-)

# 7. npx tsc --project ../ElectroShopVe-gemini/tsconfig.json --noEmit:
Exit code 0 (0 errores)

# 8. npx eslint:
39 problems (22 errors, 17 warnings) — idéntico a main (sin errores nuevos)
```

- Notas / cosas raras vistas (sin arreglarlas):
  - En `orders/page.tsx` y `balance/page.tsx` se preservaron intactas ambas vistas responsive (MOBILE VIEW `lg:hidden` y DESKTOP VIEW `hidden lg:block`) según manda la regla de la tarea.
  - La tarjeta principal de saldo se mantuvo como bloque sólido `bg-brand-600 text-white`.
  - En `RechargeModalV2.tsx`, no se modificó la lógica de recarga, conservando la tasa vinculada a `/api/settings/public` (`exchangeRateVES`) de C-72.
  - Se eliminó la función no utilizada `getMethodColor` en `RechargeModalV2.tsx`.
  - En `digital/page.tsx`, se añadió `sizes="48px"` al `<Image ... fill>` del producto.
