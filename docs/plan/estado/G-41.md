# G-41
Estado: HECHO
Rama: gemini/R11
- Archivos: 3 (`app/checkout/page.tsx`, `components/checkout/CheckoutPagoMovilForm.tsx`, `components/pago-movil/VerificarPagoMovilForm.tsx`)
- Verificación:
  - Banned patterns (`bg-gradient`, `backdrop-blur`, `animate-bounce`, `animate-ping`, `z-[9999]`, `font-black`, `font-extrabold`): 0
  - Banned typography (`text-[7px]`, `text-[8px]`, `text-[9px]`, `text-[9.5px]`, `text-[10px]`): 0
  - Colores sueltos (`slate-`, `blue-`, `emerald-`, `amber-`, etc.): 0
  - `<Image fill ...>` sin `sizes`: 0
  - `<style jsx>`: 0
- tsc: sin errores nuevos (`npx tsc --noEmit` código de salida 0)
- Arreglos aplicados:
  - `checkout/page.tsx`:
    - Pantallas de carga, vacía y sin autenticación unificadas con `min-h-dvh bg-surface`, `adminSpinner`, `adminCard` y textos con tokens `text-ink` / `text-muted`.
    - Montos mostrados con `toFixed` migrados a `formatUSD` y `formatVES` (`giftCardInfo.balanceUSD`, `finalTotal`, `userBalance`, `packagingFee`, `consolidatedCost`, `item.cost`, `bulkyCost`, y tasa BCV). Preservados los pesos `kg` y el query param en la línea 477.
    - Modal de términos implementado con `adminModalOverlay`, `adminModalPanel` y `useBodyScrollLock(showTermsModal)`.
    - Textos blancos del bloque de envío pasaron a `text-ink`.
  - `CheckoutPagoMovilForm.tsx`:
    - Tarjeta de datos para transferir y montos modernizados con tokens `bg-surface`, `adminCard`, `adminSuccessButton`.
    - Modal QR refactorizado con `adminModalOverlay`, `adminModalPanel` y capa `z-[var(--z-modal)]`.
    - Eliminado `<style jsx>` muerto.
  - `VerificarPagoMovilForm.tsx`:
    - Colores de estados y errores migrados a tokens `deal`, `warning`, `success` y `adminPrimaryButton`.
- Notas / cosas raras vistas (sin arreglarlas):
  - Lógica de pedidos, pagos, validaciones de teléfono/referencia y comunicación con APIs preservada 100% intacta.
