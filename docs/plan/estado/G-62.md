# G-62 · Dos arreglos pendientes de C-98
Estado: HECHO
Rama: gemini/R20
- Archivos: 2

## 1. ESLint por archivo antes y después

### Antes:
- `components/modals/ConfirmDialog.tsx`: {}
- `app/customer/(dashboard)/profile/page.tsx`: {"react-hooks/set-state-in-effect":1,"react-hooks/immutability":2}

### Después:
- `components/modals/ConfirmDialog.tsx`: {}
- `app/customer/(dashboard)/profile/page.tsx`: {"react-hooks/set-state-in-effect":1,"react-hooks/immutability":2}

## 2. Verificación de greps
```
$ grep -n "whitespace-pre-line" components/modals/ConfirmDialog.tsx
114:                    <p className="text-sm text-muted leading-relaxed whitespace-pre-line break-words">

$ grep -n "businessVerificationNotes: data" "app/customer/(dashboard)/profile/page.tsx"
197:          businessVerificationNotes: data.profile?.businessVerificationNotes || '',
```

## 3. npx tsc --noEmit (salida real)
```
$ npx tsc --noEmit
(salida vacía, 0 errores, código de salida 0)
```

## 4. npm run build (últimas 5 líneas)
```
The column `existe` does not exist in the current database.
    at async (.next/server/chunks/5460.js:1:7905)
    at async v (.next/server/chunks/5460.js:1:17581) {
  code: 'P2022',
  meta: { modelName: 'CompanySettings', column: 'existe' },
  clientVersion: '6.19.3'
}
⨯ Next.js build worker exited with code: 1 and signal: null
```

## 5. git diff --stat contra git diff -w --stat

### git diff --stat:
```
 app/customer/(dashboard)/profile/page.tsx | 1 +
 components/modals/ConfirmDialog.tsx       | 2 +-
 2 files changed, 2 insertions(+), 1 deletion(-)
```

### git diff -w --stat:
```
 app/customer/(dashboard)/profile/page.tsx | 1 +
 components/modals/ConfirmDialog.tsx       | 2 +-
 2 files changed, 2 insertions(+), 1 deletion(-)
```
