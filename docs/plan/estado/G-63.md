# G-63 · Borrar ProductForm.tsx (sin uso)
Estado: HECHO
Rama: gemini/R20
- Archivos: 1 borrado

## 1. ESLint por archivo antes y después

### Antes:
- `app/admin/(dashboard)/products/_components/ProductForm.tsx`: {"@typescript-eslint/no-explicit-any":4,"react-hooks/immutability":2,"react-hooks/exhaustive-deps":1,"@typescript-eslint/no-unused-vars":1}

### Después:
- Archivo eliminado (0 errores)

## 2. Verificación de git grep
```
$ git grep -n "ProductForm" -- app components lib
app/admin/(dashboard)/products/_components/ProductForm.tsx:48:export default function ProductForm({ productId }: Props) {
```
(Solo sale la definición del propio archivo antes de ser eliminado, 0 usos fuera)

## 3. npx tsc --noEmit (salida real)
```
$ npx tsc --noEmit
(salida vacía, 0 errores, código de salida 0)
```

## 4. npm run build (últimas 5 líneas)
```
The column `existe` does not exist in the current database.
    at async (.next/server/chunks/5460.js:1:7905)
    at async v (.next/server/chunks/5460.js:1:17581) {
  code: 'P2022',
  meta: { modelName: 'CompanySettings', column: 'existe' },
  clientVersion: '6.19.3'
}
⨯ Next.js build worker exited with code: 1 and signal: null
```

## 5. git diff --stat contra git diff -w --stat

### git diff --stat:
```
 .../products/_components/ProductForm.tsx           | 1057 --------------------
 1 file changed, 1057 deletions(-)
```

### git diff -w --stat:
```
 .../products/_components/ProductForm.tsx           | 1057 --------------------
 1 file changed, 1057 deletions(-)
```
