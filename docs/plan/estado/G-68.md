# G-68 · Mover el `useEffect` debajo de la función que llama
Estado: HECHO
Rama: gemini/R22
- Archivos: 5
- Verificación: 0 errores react-hooks/immutability; solo se movieron líneas (diff sorted limpio en los 5)
- tsc: sin errores (código de salida 0)
- build: prerender de / falla únicamente por columna existe inexistente en DB local (esperado y documentado)

---

## 1. ESLint por archivo antes y después (con el nombre de cada regla)

### Resumen comparativo:

| Archivo | Antes | Después | Resultado immutability |
|---|---|---|---|
| `app/customer/(dashboard)/page.tsx` | `set-state-in-effect` + `immutability` | solo `set-state-in-effect` | **0** (eliminado) |
| `app/customer/(dashboard)/reviews/page.tsx` | `immutability` | `set-state-in-effect` (destapado al resolver immutability) | **0** (eliminado) |
| `app/customer/(dashboard)/settings/page.tsx` | `immutability` + `set-state-in-effect` | solo `set-state-in-effect` | **0** (eliminado) |
| `app/customer/(dashboard)/warranty/page.tsx` | `immutability` + `purity` | `set-state-in-effect` (destapado) + `purity` | **0** (eliminado) |
| `app/customer/(dashboard)/profile/page.tsx` | `set-state-in-effect` + 2 `immutability` | solo `set-state-in-effect` | **0** (ambos eliminados) |

Total `react-hooks/immutability` en tu carril: **0**.

---

### Detalle Antes:

#### 1. `app/customer/(dashboard)/page.tsx`
- Línea 52: `react-hooks/set-state-in-effect` (Calling setState synchronously within an effect)
- Línea 55: `react-hooks/immutability` (Cannot access variable before it is declared: `fetchDashboardData`)
(2 errores)

#### 2. `app/customer/(dashboard)/reviews/page.tsx`
- Línea 34: `react-hooks/immutability` (Cannot access variable before it is declared: `fetchMyReviews`)
(1 error)

#### 3. `app/customer/(dashboard)/settings/page.tsx`
- Línea 81: `react-hooks/immutability` (Cannot access variable before it is declared: `fetchSettings`)
- Línea 84: `react-hooks/set-state-in-effect` (Calling setState synchronously within an effect)
(2 errores)

#### 4. `app/customer/(dashboard)/warranty/page.tsx`
- Línea 74: `react-hooks/immutability` (Cannot access variable before it is declared: `fetchDeliveredOrders`)
- Línea 96: `react-hooks/purity` (Cannot call impure function during render: `Date.now`)
(2 errores)

#### 5. `app/customer/(dashboard)/profile/page.tsx`
- Línea 159: `react-hooks/set-state-in-effect` (Calling setState synchronously within an effect: `setIsMounted(true)`)
- Línea 160: `react-hooks/immutability` (Cannot access variable before it is declared: `fetchProfile`)
- Línea 161: `react-hooks/immutability` (Cannot access variable before it is declared: `fetchStats`)
(3 errores)

---

### Detalle Después:

#### 1. `app/customer/(dashboard)/page.tsx`
```
/home/andruwxox/Escritorio/ElectroShopVe-gemini/app/customer/(dashboard)/page.tsx
  67:20  error  Error: Calling setState synchronously within an effect can trigger cascading renders  react-hooks/set-state-in-effect

✖ 1 problem (1 error, 0 warnings)
```

#### 2. `app/customer/(dashboard)/reviews/page.tsx`
```
/home/andruwxox/Escritorio/ElectroShopVe-gemini/app/customer/(dashboard)/reviews/page.tsx
  49:5  error  Error: Calling setState synchronously within an effect can trigger cascading renders  react-hooks/set-state-in-effect

✖ 1 problem (1 error, 0 warnings)
```

#### 3. `app/customer/(dashboard)/settings/page.tsx`
```
/home/andruwxox/Escritorio/ElectroShopVe-gemini/app/customer/(dashboard)/settings/page.tsx
  121:5  error  Error: Calling setState synchronously within an effect can trigger cascading renders  react-hooks/set-state-in-effect

✖ 1 problem (1 error, 0 warnings)
```

#### 4. `app/customer/(dashboard)/warranty/page.tsx`
```
/home/andruwxox/Escritorio/ElectroShopVe-gemini/app/customer/(dashboard)/warranty/page.tsx
  91:5   error  Error: Calling setState synchronously within an effect can trigger cascading renders  react-hooks/set-state-in-effect
  96:18  error  Error: Cannot call impure function during render                                       react-hooks/purity

✖ 2 problems (2 errors, 0 warnings)
```

#### 5. `app/customer/(dashboard)/profile/page.tsx`
```
/home/andruwxox/Escritorio/ElectroShopVe-gemini/app/customer/(dashboard)/profile/page.tsx
  236:5  error  Error: Calling setState synchronously within an effect can trigger cascading renders  react-hooks/set-state-in-effect

✖ 1 problem (1 error, 0 warnings)
```

---

## 2. Diff ordenado y `git diff --stat`

### Verificación de ordenación (solo se movieron líneas):
```bash
$ for f in app/customer/(dashboard)/page.tsx app/customer/(dashboard)/reviews/page.tsx app/customer/(dashboard)/settings/page.tsx app/customer/(dashboard)/warranty/page.tsx app/customer/(dashboard)/profile/page.tsx; do diff <(git show gemini/R21:"" | sort) <(sort "") && echo ": solo se movieron líneas"; done
app/customer/(dashboard)/page.tsx: solo se movieron líneas
app/customer/(dashboard)/reviews/page.tsx: solo se movieron líneas
app/customer/(dashboard)/settings/page.tsx: solo se movieron líneas
app/customer/(dashboard)/warranty/page.tsx: solo se movieron líneas
app/customer/(dashboard)/profile/page.tsx: solo se movieron líneas
```

### `git diff --stat`:
```
 app/customer/(dashboard)/page.tsx          | 16 ++++++++--------
 app/customer/(dashboard)/profile/page.tsx  | 24 ++++++++++++------------
 app/customer/(dashboard)/reviews/page.tsx  |  8 ++++----
 app/customer/(dashboard)/settings/page.tsx | 16 ++++++++--------
 app/customer/(dashboard)/warranty/page.tsx |  8 ++++----
 5 files changed, 36 insertions(+), 36 deletions(-)
```

---

## 3. npx tsc --noEmit (salida real)
```
$ npx tsc --noEmit
(salida vacía, 0 errores, código de salida 0)
```

---

## 4. npm run build (últimas 5 líneas)
```
The column `existe` does not exist in the current database.
    at async (.next/server/chunks/5460.js:1:7907)
    at async v (.next/server/chunks/5460.js:1:17583) {
  code: 'P2022',
  meta: { modelName: 'CompanySettings', column: 'existe' },
  clientVersion: '6.19.3'
}
⨯ Next.js build worker exited with code: 1 and signal: null
```
